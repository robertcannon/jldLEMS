These are the runtime classes for executing a model as in the original implementation of jLEMS.

This is mainly useful as a reference implementation. Other branches of the pipeline, such as those to LEMSLite or other code 
generation are likely to provide more efficient implementations.  